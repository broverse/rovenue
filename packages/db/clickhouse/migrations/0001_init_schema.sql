-- 0001_init_schema.sql
-- Initial rovenue database. The migration runner creates the database
-- programmatically before reading this file; this statement is a
-- self-documenting no-op that leaves the migration chain intact and
-- makes the directory readable standalone.
CREATE DATABASE IF NOT EXISTS rovenue;
