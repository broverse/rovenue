# Origin of the dashboard app (where Better Auth login lives).
# Login/CTA buttons link to ${PUBLIC_DASHBOARD_URL}/login.
# Defaults to the local dev dashboard port when unset.
PUBLIC_DASHBOARD_URL=http://localhost:5173
