/**
 * Outbound links used across the landing page. Swap these for your real
 * dashboard / auth / repo URLs (or wire them to env vars at build time).
 */
export const siteLinks = {
  github: 'https://github.com/rovenue/rovenue',
  signIn: '/sign-in',
  getStarted: '/sign-in',
  docs: 'https://docs.rovenue.app',
} as const;

export const githubStars = '8.4k';
