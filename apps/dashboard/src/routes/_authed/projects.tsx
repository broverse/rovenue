import { createFileRoute } from "@tanstack/react-router";

// Placeholder. Full list + create flow lands in Task B7.
export const Route = createFileRoute("/_authed/projects")({
  component: () => null,
});
