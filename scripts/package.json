{
  "name": "@rovenue/scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "rotate-encryption-key": "tsx rotate-encryption-key.ts",
    "verify:asset-headers": "tsx --env-file-if-exists=../.env verify-asset-headers.ts",
    "verify:audit-bundle": "tsx verify-audit-bundle.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --minWorkers=1 --maxWorkers=2"
  },
  "dependencies": {
    "@rovenue/db": "workspace:*",
    "@rovenue/shared": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1097.0",
    "@types/node": "^22.0.0",
    "testcontainers": "^10.15.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.6"
  }
}
