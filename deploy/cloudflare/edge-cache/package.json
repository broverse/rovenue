{
  "name": "rovenue-edge-cache",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that edge-caches Rovenue's SDK read endpoints (offerings, config) with per-project KV-versioned purge.",
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260616.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.90.0"
  }
}
